/**
 * Types for the ported interaction module (lib/interactions.js).
 * The implementation stays plain JS so it remains a line-for-line port of the
 * static build's behaviour — only the surface is typed.
 */
export declare function initInteractions(): () => void;
